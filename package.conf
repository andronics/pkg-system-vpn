name="vpn"
description="VPN Manager - NordVPN control and server selection"
version="0.1.0"
method="stow"
collection="system"
fold=false

# Ignore documentation files
supported_os=("linux")
depends=()
requires_commands=("curl" "dmenu" "nordvpn" "systemctl")
ignore=(
    "README.md"
)
